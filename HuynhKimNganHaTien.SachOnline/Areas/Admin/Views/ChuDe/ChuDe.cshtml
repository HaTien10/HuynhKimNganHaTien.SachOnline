@model IEnumerable<HuynhKimNganHaTien.SachOnline.Models.CHUDE>

@{
    ViewBag.Title = "ChuDe";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>ChuDe</h2>

<p>
    @Html.ActionLink("Create New", "insert")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenChuDe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChuDe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCD }) |
            @Html.ActionLink("Details", "ChiTiet", new { id=item.MaCD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCD })
        </td>
    </tr>
}

</table>
